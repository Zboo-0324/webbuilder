interface:
  display_name: "Spec2Web"
  short_description: "Gate full-stack delivery from requirements to release"
  default_prompt: "Use $spec2web to initialize or continue a gated full-stack delivery workflow."

policy:
  allow_implicit_invocation: true
