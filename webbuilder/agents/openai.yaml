interface:
  display_name: "WebBuilder"
  short_description: "Orchestrate gated multi-agent full-stack delivery"
  default_prompt: "Use $webbuilder to initialize or continue an adaptive multi-agent web delivery workflow."

policy:
  allow_implicit_invocation: true
