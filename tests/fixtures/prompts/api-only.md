# API-Only Evaluation Scenario

Build a headless REST API service with authentication, role-based access
control, database persistence, and audit logging.  There is no user-facing
UI; all interaction is through programmatic API endpoints.  The service must
be containerized and pass functional, performance, security, and deployment
smoke tests.
